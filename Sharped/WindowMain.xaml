<Window x:Class="Sharped.WindowMain"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Window1" Height="353" Width="639">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition  Height="26"></RowDefinition>
            <RowDefinition  Height="9*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Menu Height="22" Name="menu1" Grid.Row="0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top" >
            <MenuItem Header="_File">
                <MenuItem Header="_Open" />
                <MenuItem Header="_Save" />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
        </Menu>
        <ScrollViewer Grid.Row="1" >
            <RichTextBox Name="codeTextBox" FontFamily="CournerNew" FontSize="14" TextChanged="RichTextBox_TextChanged" >
                <RichTextBox.Resources>
                    <Style TargetType="{x:Type Paragraph}">
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </RichTextBox.Resources>
            </RichTextBox>
        </ScrollViewer>
        <TextBox Grid.Row="2" FontFamily="CournerNew" FontSize="14">
        </TextBox>
    </Grid>
</Window>
